import { Routes } from '@angular/router';
import { ManageRoutes } from './manage-routes';

export const MANAGE_ROUTES_ROUTES: Routes = [
    {
        path: '',
        component: ManageRoutes
    }
];