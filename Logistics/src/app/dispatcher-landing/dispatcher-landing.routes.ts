import { Routes } from '@angular/router';
import { DispatcherLanding } from './dispatcher-landing';

export const DISPATCHER_LANDING_ROUTES: Routes = [
    {
        path: '',
        component: DispatcherLanding
    }
];